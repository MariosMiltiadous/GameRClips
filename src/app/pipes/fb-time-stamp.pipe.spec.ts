import { FbTimeStampPipe } from './fb-time-stamp.pipe';

describe('FbTimeStampPipe', () => {
  it('create an instance', () => {
    const pipe = new FbTimeStampPipe();
    expect(pipe).toBeTruthy();
  });
});
